<!DOCTYPE html>
<html lang="en">
  <%- include('header') -%>

  <main>
    <link rel="stylesheet" href="/css/confirmDelete.css">
    <h2>Delete This User</h2>

    <p class="warning">Are you sure you want to delete this user?</p>

    <form method="post" action="/admin/deleteUser/<%= id %>" class="confirm-form">
      <div class="field">
        <strong>ID:</strong> <span><%= id %></span>
      </div>
      <div class="field">
        <strong>Name:</strong> <span><%= name %></span>
      </div>
      <div class="field">
        <strong>Acronym:</strong> <span><%= acronym %></span>
      </div>

      <div class="form-actions">
        <input type="submit" name="doit" value="Delete" class="danger-button">
        <a href="/admin" class="cancel-link">Cancel</a>
      </div>
    </form> 

    
  </main>
  <%- include('footer') -%>
</html>
